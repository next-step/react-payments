export type CardNumber = {
  firstNumber?: string;
  secondNumber?: string;
  thirdNumber?: string;
  fourthNumber?: string;
};

export type ExpirationDate = {
  month: string;
  year: string;
};

export type TwoPasswordDigits = {
  first: string;
  second: string;
};
