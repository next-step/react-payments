import styled from 'styled-components'

const CardCreateContainer = styled.div`
  margin-top: 25px;
  display: flex;
  flex-direction: column;
  align-items: center;
`

export default {
  CardCreateContainer,
}
