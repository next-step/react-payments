const UI_VARIANT = {
  FILLED: 'filled',
  OUTLINED: 'outlined',
  GHOST: 'ghost',
} as const

const UI_TYPE = {
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
  DANGER: 'danger',
} as const

const UI_SIZE = {
  SMALL: 'small',
  MEDIUM: 'medium',
  LARGE: 'large',
} as const

export { UI_VARIANT, UI_TYPE, UI_SIZE }
