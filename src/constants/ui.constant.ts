const UI_VARIANT = {
  FILL: 'fill',
  OUTLINE: 'outline',
  GHOST: 'ghost',
} as const

const UI_SIZE = {
  SMALL: 'small',
  MEDIUM: 'medium',
  LARGE: 'large',
} as const

const UI_TEXT_ALIGN = {
  CENTER: 'center',
  LEFT: 'left',
  RIGHT: 'right',
} as const

export { UI_VARIANT, UI_SIZE, UI_TEXT_ALIGN }
