const UI_VARIANT = {
  FILL: 'fill',
  OUTLINE: 'outline',
  GHOST: 'ghost',
} as const

const UI_SIZE = {
  SMALL: 'small',
  MEDIUM: 'medium',
  LARGE: 'large',
} as const

export { UI_VARIANT, UI_SIZE }
