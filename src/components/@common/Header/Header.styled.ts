import styled from '@emotion/styled';
import { Box } from 'components/@common';

export const Header = styled(Box)`
  padding: 20px;
  user-select: none;

  svg {
    margin-right: 15px;
    cursor: pointer;
  }
`;
