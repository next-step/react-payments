import { Canvas, Description, Meta, Source, Story, Title } from '@storybook/blocks'

import * as InputStories from './input.stories';

<Meta of={InputStories} />

<Title/>

headless input primitive component

<Canvas>
  <Story/>
</Canvas>


## Input.Wrapper

- `input` 요소와 기타 요소들을 감싸는 레이아웃 컴포넌트
- `Flex` 컴포넌트와 props가 동일

<br/>

## Input.Content

- `input` 컴포넌트

<br/>

## Input.Label

- `input`과 같이 쓰이는 `label` 컴포넌트