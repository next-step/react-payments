import { styled } from '@/lib/stitches.config';

const ScrollArea = styled('div', {
  overflow: 'scroll',
  padding: '$3',
  margin: '0 $12',
  width: '100%',
  height: '$12',
});

export default ScrollArea;
