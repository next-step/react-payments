import React from 'react';
import { useCardListState } from '../../context/CardListContext';

const ModalManageCard = () => {
  const cardList = useCardListState();

  return <div>manage card</div>;
};

export default ModalManageCard;
