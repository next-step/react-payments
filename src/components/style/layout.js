import styled from "@emotion/styled";

const Title = styled.h1`
  position: relative;
  margin-bottom: 25px;
  font-size: 16px;
  font-weight: normal;
  color: #383838;
`;

const Main = styled.main`
  margin: auto;
`;

export { Title, Main };
