import styled from "@emotion/styled";

const Title = styled.h1`
  position: relative;
  font-size: 16px;
  font-weight: normal;
  color: #383838;
`;

const Main = styled.main`
  margin: auto;
`;

export { Title, Main };
