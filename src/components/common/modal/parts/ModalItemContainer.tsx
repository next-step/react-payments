import {type PropsWithChildren} from 'react';

const ModalItemContainer = ({children}: PropsWithChildren) => <div className='modal-item-container'>{children}</div>;

export default ModalItemContainer;
