import type { ChildrenProps } from 'components';

const InputLabel = ({ children }: ChildrenProps) => {
  return <span className="input-label">{children}</span>;
};

export default InputLabel;
