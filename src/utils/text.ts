export const upperCase = (text: string) => {
  return text.toUpperCase();
};

export const lowerCase = (text: string) => {
  return text.toLowerCase();
};

export const length = (text: string) => {
  return text.length;
};
