import { render, screen, fireEvent } from '@testing-library/react';
import { BrowserRouter } from 'react-router-dom';

import CardRegistrationPage from './CardRegistrationPage';

describe('Card registration page test', () => {
  it('should render CardRegistrationPage.', async () => {});
});

describe('4. Modal test', () => {
  it('4-1. should open the modal as the Card component is clicked.', async () => {});
  it('4-2. should close the modal by clicking the background or selecting a company.', () => {});
  it('4-3. should have scroll rocked with the modal open.', () => {});
  it('4-4. should the Card component change its background color to the one that has selected.', () => {});
});
