import CompanySelectionModal from './modal';

export default {
  title: 'Components/Modal',
  component: CompanySelectionModal,
};

const Template = (args) => <CompanySelectionModal {...args} />;

export const Default = Template.bind({});
